/**
 * Project file and folder setup
 * 1. Create HTML file and script link with output script.js
 * 2. Create script.ts file
 * 3. Create configuration tsconfig.json file by command `tsc --init`
 * 4. Change tscconfig.json --> rootDir ("./src") , outDir ("./output")
 * 5. Now file can compile by `tsc` command, if you want watch mode command `tsc -w`
 *
 *
 */
